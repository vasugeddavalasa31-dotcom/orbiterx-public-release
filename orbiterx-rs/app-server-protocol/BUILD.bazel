load("//:defs.bzl", "orbiterx_rust_crate")

orbiterx_rust_crate(
    name = "app-server-protocol",
    crate_name = "orbiterx_app_server_protocol",
    test_data_extra = glob(
        ["schema/**"],
        allow_empty = True,
    ),
)
