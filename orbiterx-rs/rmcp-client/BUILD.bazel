load("//:defs.bzl", "orbiterx_rust_crate")

orbiterx_rust_crate(
    name = "rmcp-client",
    crate_name = "orbiterx_rmcp_client",
    extra_binaries = [
        "//orbiterx-rs/cli:orbiterx",
    ],
)
