load("//:defs.bzl", "orbiterx_rust_crate")

orbiterx_rust_crate(
    name = "models-manager",
    compile_data = [
        "models.json",
        "prompt.md",
    ],
    crate_name = "orbiterx_models_manager",
)
