load("//:defs.bzl", "orbiterx_rust_crate")

orbiterx_rust_crate(
    name = "core-plugins",
    compile_data = glob(
        include = ["**"],
        allow_empty = True,
        exclude = [
            "**/* *",
            "BUILD.bazel",
            "Cargo.toml",
        ],
    ),
    crate_name = "orbiterx_core_plugins",
)
