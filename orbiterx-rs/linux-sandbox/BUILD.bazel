load("//:defs.bzl", "orbiterx_rust_crate")

orbiterx_rust_crate(
    name = "linux-sandbox",
    crate_name = "orbiterx_linux_sandbox",
    extra_binaries = [
        "//orbiterx-rs/bwrap:bwrap",
    ],
)
