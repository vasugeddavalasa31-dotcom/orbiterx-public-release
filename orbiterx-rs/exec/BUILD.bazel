load("//:defs.bzl", "orbiterx_rust_crate")

orbiterx_rust_crate(
    name = "exec",
    crate_name = "orbiterx_exec",
    test_shard_counts = {
        "exec-all-test": 8,
    },
    test_tags = ["no-sandbox"],
)
