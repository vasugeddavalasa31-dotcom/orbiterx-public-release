load("//:defs.bzl", "orbiterx_rust_crate")

orbiterx_rust_crate(
    name = "goal",
    compile_data = glob([
        "templates/**",
    ]),
    crate_name = "orbiterx_goal_extension",
    integration_compile_data_extra = ["src/accounting.rs"],
)
