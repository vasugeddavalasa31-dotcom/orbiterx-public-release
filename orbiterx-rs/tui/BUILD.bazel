load("//:defs.bzl", "MACOS_WEBRTC_RUSTC_LINK_FLAGS", "orbiterx_rust_crate")

orbiterx_rust_crate(
    name = "tui",
    compile_data = glob(
        include = ["**"],
        allow_empty = True,
        exclude = [
            "**/* *",
            "BUILD.bazel",
            "Cargo.toml",
        ],
    ) + [
        "//orbiterx-rs/collaboration-mode-templates:templates/default.md",
        "//orbiterx-rs/collaboration-mode-templates:templates/plan.md",
    ],
    crate_name = "orbiterx_tui",
    extra_binaries = [
        "//orbiterx-rs/cli:orbiterx",
    ],
    integration_compile_data_extra = ["src/test_backend.rs"],
    rustc_flags_extra = MACOS_WEBRTC_RUSTC_LINK_FLAGS,
    test_data_extra = glob([
        "src/**/*.rs",
        "src/**/snapshots/**",
    ]),
    test_shard_counts = {
        "tui-unit-tests": 8,
    },
)
