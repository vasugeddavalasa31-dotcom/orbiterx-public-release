[package]
name = "orbiterx-thread-manager-sample"
version.workspace = true
edition.workspace = true
license.workspace = true

[lints]
workspace = true

[dependencies]
anyhow = { workspace = true }
clap = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
# Keep this sample limited to a single OrbiterX workspace dependency.
# Add new OrbiterX surface area to `orbiterx-core-api` instead of depending on
# additional `orbiterx-*` crates here.
orbiterx-core-api = { workspace = true }
tracing = { workspace = true }
